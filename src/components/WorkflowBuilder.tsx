"use client";

import React from "react";

/**
 * Minimal stub for Step 4. Step 6 will replace with full React Flow canvas + Sidebar.
 */
export default function WorkflowBuilder() {
  return (
    <div className="relative h-full min-h-[60vh] w-full overflow-hidden bg-neutral-950">
      <div className="flex h-full items-center justify-center text-neutral-500">
        Workflow editor — canvas and sidebar coming in Step 6.
      </div>
    </div>
  );
}
